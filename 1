// SPDX-License-Identifier: GPL-3.0
pragma solidity >= 0.5.0 < 0.9.0;

contract stringConcatenation {
    function concatenate(string memory x, string memory y) public pure returns(string memory) {
        return string.concat(x, y);
    }
}
